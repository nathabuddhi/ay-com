export interface NotificationSettings {
    notif_like: boolean;
    notif_repost: boolean;
    notif_follow: boolean;
    notif_mention: boolean;
    notif_community: boolean;
    notif_newsletter: boolean;
}